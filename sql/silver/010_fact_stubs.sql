-- =============================================================================
-- Silver 010 — Fact table stubs (no-op)
-- fact_batting and fact_pitching require /v1/people/{id}/stats API data which
-- is not yet extracted into bronze.
-- TODO(M6): replace with real transforms once bronze/stats/ Parquet files exist.
-- =============================================================================

SELECT 1 WHERE 1 = 0;
